第 1 步：项目骨架与无障碍底层（地基）
Prompt 1:
“请用纯前端技术（HTML + CSS + Vanilla JS）创建一个单页应用（SPA），项目名：AudioBraille。

核心要求：

无障碍优先：所有交互元素原生支持键盘（Tab 导航）。为动态内容添加 aria-live="polite" 区域。默认字号≥20px，默认高对比度（黑底黄字）。页面加载后，自动聚焦到主内容区，并用 Web Speech API 朗读：‘欢迎来到 AudioBraille 盲文学习平台’。

键盘驱动菜单：顶部导航栏包含 教学、盲文输入器、AudioBraille实验、笔记记录器 四个模块，通过 Tab 和 Enter 切换，不依赖鼠标。

全局状态管理：创建一个全局状态对象 AppState，包含：currentPage（当前页面）、brailleDots（长度为6的布尔数组，分别对应点1-6）、learningProgress（学习进度）、notes（笔记列表）。

音频上下文激活（极其重要）：页面加载后，监听用户的第一次键盘敲击（任意键），立即执行 audioContext.resume() 并创建一个极短的静音缓冲区以解锁 Web Audio API（规避 Chrome 自动播放策略）。激活后，语音提示‘音频引擎已就绪’。

语音封装：封装全局函数 speak(text)，调用 Web Speech API，并设置 rate=0.9，pitch=1，确保中文发音清晰。”

第 2 步：盲文核心引擎（不标调 + 候选词逻辑）
Prompt 2:
“请编写 braille-engine.js 核心逻辑模块。特别注意：本项目不实现盲文声调输入，仅处理声母和韵母。

映射表构建：

创建英文字母（a-z）到盲文点位（1-6数组）的映射表。例如：a: [1], b: [1,2]。

创建中文拼音声母（b, p, m, f, d, t, n, l, g, k, h, j, q, x, zh, ch, sh, r, z, c, s, y, w）到盲文点位的映射。

创建中文拼音韵母（a, o, e, i, u, ü, ai, ei, ui, ao, ou, iu, ie, üe, er, an, en, in, un, ün, ang, eng, ing, ong）到盲文点位的映射。

核心转换函数：

pointsToChar(pointsArray)：将6个布尔值转为英文字母或拼音（先查声母，再查韵母，组合成拼音）。

charToPoints(char)：反向查找。

中文候选词生成器：

创建函数 getCandidates(pinyin)，输入拼音（如 "li"），返回一个按使用频率排序的汉字数组（如 ['里', '力', '离', '理', '利']）。初期可以硬编码一个包含100个常用汉字的词库。

Unicode 盲文显示：实现函数 dotsToUnicode(pointsArray)，将6个布尔值映射为 U+2800 基础码位，用于屏幕视觉反馈（方便明眼人协助调试）。

导出一个全局对象 BrailleEngine 供其他模块调用。”

第 3 步：数字小键盘输入器（点击切换 + 官方布局）
Prompt 3:
“请实现数字小键盘盲文输入模块，采用点击切换模式（按一下亮起，再按一下熄灭）。

1. 物理映射（官方强制布局，但预留自定义接口）：

7 → 点1, 4 → 点2, 1 → 点3（左列）

8 → 点4, 5 → 点5, 2 → 点6（右列）

该布局作为默认配置存储在 localStorage 中，后续可由用户自定义。

2. 交互逻辑：

监听全局 keydown。当按下上述6个键之一时，切换 AppState.brailleDots 中对应点位（0变1，1变0）。

每次切换后，立即更新屏幕显示的6点阵图，并调用 speak() 播报“点X亮起”或“点X熄灭”。

重要：输入过程中不播放任何和弦音效。

3. 功能键定义（核心工作流）：

0（零键）：确认/送字。将当前6个点阵翻译成字符（调用 BrailleEngine.pointsToChar），将字符上屏（插入到当前焦点文本区），清空所有点阵（自动重置），并播放一次完整的 AudioBraille 和弦（调用后续将实现的 playAudioBraille 函数），最后用 TTS 朗读该字符。

3：退格。删除上屏的最后一个字符或盲文方。

-（减号）：一键清空。立即清空当前所有点阵，并语音提示“已清空”。

+（加号）：朗读当前点阵。语音播报当前哪几个点位亮起（如“点1、点4、点6”）。

.（句号）：插入空格。

9：下一个候选词（在有候选列表时下翻）。

6：上一个候选词（在有候选列表时上翻）。

4. 候选词交互：当用户输入拼音并确认后，底部弹出候选词栏。用户用 9 和 6 切换高亮，按 0 确认选定，所选词上屏并清空点阵。”

第 4 步：AudioBraille 空间音频核心（仅确认时触发）
Prompt 4:
“请实现 AudioBraille 空间音频模块，这是项目的核心创新点，且仅在用户按 0 确认时触发播放。

技术实现（Web Audio API）：

创建全局音频上下文（已在第一步激活）。

编码规则（硬编码）：

空间定位（左右声道）：左列点（点1,2,3）使用 PannerNode 将声音定位到 -90 度（左耳）；右列点（点4,5,6）定位到 +90 度（右耳）。

音高映射（上中下）：上行（点1,4）= 600Hz；中行（点2,5）= 400Hz；下行（点3,6）= 250Hz。

播放函数 playAudioBraille(dotsArray)：

传入当前的6个布尔点阵。

遍历6个点，如果该点位为 true，则创建一个振荡器（OscillatorNode），设置对应频率，连接对应的 PannerNode，再连接到音频上下文。

所有“有点”的音符同时响起，形成一个短促的立体声和弦（持续时长 0.25 秒）。

播放时，屏幕上的对应点位视觉高亮闪烁，以匹配听觉反馈。

导出全局函数供第3步调用。”

第 5 步：交互式教学模块（探索 + 考试 + 间隔重复）
Prompt 5:
“请实现教学模块，包含两种模式，所有操作键盘驱动。

1. 探索模式（学习）：

屏幕中央显示一个当前学习的字母/拼音（如“A”或“妈”）。

系统语音朗读该字符。

用户可以使用小键盘自由按 7,4,1,8,5,2 探索其盲文点位，系统实时播报点亮的点位名称。

按 0 核对：系统判断当前点阵是否正确，并播报“正确”或“错误，正确答案是点X...”。

2. 考试模式（测验）：

系统语音随机播报一个字母或拼音（如“请打出字母 B”），用户输入盲文后按 0 提交。

系统判断对错，记录本次正确率。

3. 间隔重复错题训练（核心算法）：

使用 localStorage 存储每个字符的错误次数和上次错误时间。

当错误次数 ≥ 2 时，将该字符自动加入“错题本”。

在考试模式中，每5道题强制插入1道错题本中的题目，直到该字符连续正确3次后移出错题本。

课程进度保存：当前学到第几关、正确率自动存入 localStorage，刷新页面后按 Ctrl+R（或任何键）自动恢复进度。”

第 6 步：无纸化盲文记录器 + 实验模式整合（最终打磨）
Prompt 6:
“请实现最后的笔记记录器和实验模式，并整合全部模块。

1. 笔记记录器：

提供一个多行文本编辑区（contenteditable 或 <textarea>），完全键盘可访问。

用户在小键盘输入盲文，按 0 确认后文字上屏到光标位置。

盲文原码存储：每次保存笔记时，不仅保存明文（如“你好”），同时保存对应的盲文点位序列（如 [[1,2], [2,3]]）。

快捷键：Ctrl + S 保存（存入 localStorage 并按时间戳命名）；Ctrl + O 打开历史列表（键盘上下键选择，Enter 打开）；Ctrl + N 新建笔记。

盲文回放：在打开的历史笔记中，按 + 键，系统不仅朗读明文，还能调用 playAudioBraille 函数，把整篇笔记的盲文序列逐个“演奏”出来（每个方间隔0.3秒）。

2. 实验模式（验证 AudioBraille 效率）：

在导航栏增加“实验模式”。

随机显示10个字母，用户需用两种方式识别：A组（传统TTS直接朗读字母名），B组（通过 AudioBraille 播放空间音频，但不显示文字）。

系统自动记录每组识别的总耗时和正确率。

实验结束后，屏幕和语音播报对比结果（例如：‘语音组 10秒，AudioBraille组 12秒’）。

3. 全局无障碍收尾：

全局快捷键：F1 打开帮助菜单，用语音列表朗读所有快捷键说明；Ctrl+Shift+M 切换静音模式（关闭 TTS 语音，仅保留 AudioBraille 和弦）。

全面检查 ARIA 标签，确保屏幕阅读器（NVDA/JAWS）用户可完整操作。”